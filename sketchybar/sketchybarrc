#!/usr/bin/env lua
local config_dir = os.getenv("CONFIG_DIR")

if not config_dir or config_dir == "" then
  error("CONFIG_DIR is not set; SketchyBar must provide its configuration directory")
end

dofile(config_dir .. "/init.lua")
