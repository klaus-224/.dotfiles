---
description: Deprecated; use /test-plan with an explicit PR and test type
agent: pair-programmer
---

This workflow has been retired because it referenced unavailable agents and the removed plan-store tools.

Use `/test-plan <PR-number-or-URL> <manual|unit|playwright|comma-separated-set>` instead. Do not interpret `$ARGUMENTS` as an approved or executable plan.
