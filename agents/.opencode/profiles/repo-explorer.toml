name = "repo-explorer"
description = "Codebase discovery — optimized for indexing, mapping, and architecture understanding"

[autonomy]
approval_policy = "on-request"
sandbox = "workspace-write"
multi_agent = false

[agents]
available = ["explorer"]
